if (condición booleana)
    // Instrucciones si la condición es verdadera
    var calificacion = 7;
    console.log("He realizado mi examen.");
    // Condición (si nota es mayor o igual a 7)
    if (calificacion >= 7) {
    console.log("¡Estoy aprobado!");
    }

    let score = 9
    if (score > 8){
        document.writeln('Alumno aprobado con: '+ score)
    } else {
        document.writeln('Alumno aprobado con: '+ score)
    }

    document
    let num = 100
    if (num =='100'){
        document.writeln('son iguales')

    const dinero = 10000
    let retirar = prompt('Cuanto deseas retiirar?',0);
    if(dinero < retirar){
document.writeln('No cuenta con dinero suficiente para retirar')
    }else if (retirar > 2000 && retirar <= 5000){
    document.writeln('Se puede realizar el retiro')
    }else if (retirar >= 5000){
        document.writeln('No se permite retirar mas de 5k diarios')
    }else{

    }


























































const calificaciones
let reprobado = 0 < 6;
if(reprobado < reprobado){
document.writeln ('reprobado  si es mayor 0 y menor a 6')
}else if ( > 2000 && retirar <= 5000){
document.writeln('Se puede realizar el retiro')
}else if (retirar >= 5000){
    document.writeln('No se permite retirar mas de 5k diarios')